# Database Schema & Migrations

이 폴더는 Supabase 데이터베이스 스키마와 마이그레이션 파일을 포함합니다.

## 구조

- `migrations/`: 데이터베이스 마이그레이션 SQL 파일
- `seeds/`: 초기 데이터 시드 파일
- `schema.sql`: 전체 데이터베이스 스키마

## 사용 방법

1. Supabase 프로젝트 생성
2. SQL Editor에서 migrations 폴더의 파일들을 순서대로 실행
3. seeds 폴더의 파일들을 실행하여 초기 데이터 입력

